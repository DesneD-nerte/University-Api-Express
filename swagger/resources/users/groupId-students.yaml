get:
  summary: Returns students by groupId
  tags: 
    - Users
  security: 
    - bearerAuth: []
  parameters:
    - name: groupId
      in: path
      required: true
      description: Certain id of group
      schema:
        type : string
  responses: 
    '200':
      description: OK
      content: 
        application/json:
          schema: 
            type: array
            items: 
              $ref: '../../schemas/UserMainDesc.yaml'
    "401": 
      $ref: '../../responses/UnauthorizedError.yaml'