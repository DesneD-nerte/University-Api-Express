get:
  summary: Returns all roles
  tags: 
    - Api
  responses:
    '200':
      description: A JSON of all roles
      content:
        application/json:
          schema: 
            type: array
            items:
              $ref: '../../schemas/Role.yaml'
