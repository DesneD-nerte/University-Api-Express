get:
  summary: Returns all faculties
  tags: 
    - Api
  responses:
    '200':
      description: A JSON of all faculties
      content:
        application/json:
          schema: 
            type: array
            items:
              $ref: '../../schemas/Faculty.yaml'
