get:
  summary: Returns all lessons
  tags: 
    - Api
  responses:
    '200':
      description: A JSON of all lessons
      content:
        application/json:
          schema: 
            type: array
            items:
              $ref: '../../schemas/Lesson.yaml'
